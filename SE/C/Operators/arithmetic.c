// 1. Arithmetic operators: +, -, *, /, % (modulus)

#include<stdio.h> // linking section

int PI = 3.14; // global declaration

void main()
 {
     int num1 = 10 , num2 = 20, total;
     // total = num1 + num2;
     // total = num1 - num2;
     // total = num1 / num2;
     // total = num1 * num2;
     // total = num1 % num2;
     printf("Total = %d", total);

 }
