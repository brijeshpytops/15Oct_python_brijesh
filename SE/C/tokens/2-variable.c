What is variables in c?

Variables are used to store data values. In C, variables are declared and initialized at the same time.

syntax:

data_type variable_name = initial_value;

Example:

int age = 25;
float price = 299.0

int num1, num2; // declared
int num1 = 10, num2 = 20; // initialized
